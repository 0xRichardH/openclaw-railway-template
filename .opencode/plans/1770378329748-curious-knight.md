# Plan: Add mcporter and clawhub to mise.toml

## Summary
Add two npm packages to `docker/mise.toml` for tool management via mise.

## Changes

**File:** `docker/mise.toml`

Add these two lines to the `[tools]` section:
```toml
"npm:mcporter" = "latest"
"npm:clawhub" = "latest"
```

## Final Result
```toml
[settings]
npm.bun = true

[tools]
bun = "latest"
"npm:opencode-ai" = "latest"
"npm:@openai/codex" = "latest"
"npm:@anthropic-ai/claude-code" = "stable"
"npm:agent-browser" = "latest"
"npm:mcporter" = "latest"
"npm:clawhub" = "latest"
```

## Verification
After editing, run:
```bash
cd docker && mise install
```

This will install both tools via bun (per `npm.bun = true` setting).
